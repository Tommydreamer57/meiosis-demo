import React from 'react';

export default function create(update) {
    // INITIAL DATA
    // CHILDREN
    // FUNCTIONS
    // COMPONENT
    return {
        view(model) {
            return (
                <div>

                </div>
            );
        }
    };
}
